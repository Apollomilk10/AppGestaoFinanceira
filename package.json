{
  "name": "casa-mais",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:android": "npm run build && npx cap sync android",
    "apk": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "vercel-build": "vite build --outDir .vercel/output/static --emptyOutDir && node scripts/vercel-output.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "firebase": "^10.12.4",
    "lucide-react": "^0.436.0",
    "papaparse": "^5.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.2",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.1"
  }
}